# Database
DATABASE_URL="postgresql://user:password@localhost:5432/alafia"

# Auth — used to sign session JWTs (generate a long random string)
JWT_SECRET="replace-with-a-long-random-secret"

# Cloudinary — medical record attachments (optional until file uploads are enabled)
CLOUDINARY_CLOUD_NAME=""
CLOUDINARY_API_KEY=""
CLOUDINARY_API_SECRET=""
# Or use a single URL instead of the three vars above:
# CLOUDINARY_URL="cloudinary://api_key:api_secret@cloud_name"

# Public site URL (used for metadata / absolute links)
NEXT_PUBLIC_SITE_URL="http://localhost:3000"
